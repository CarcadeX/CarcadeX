version=lastest

signing.gnupg.executable=gpg.exe
signing.gnupg.useLegacyGpg=false
signing.gnupg.keyName=secret
signing.gnupg.passphrase=secret


ossrhUsername=secret
ossrhPassword=secret

nexusUsername=secret
nexusPassword=secret
sonatype.stagingProfileId=secret

password=secret